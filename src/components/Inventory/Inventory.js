import React from 'react';

const Inventory = () => {

    const handleAddInventory =() =>{
     
    }
    return (
        <div>
            <h1 style={{textAlign:"center"}}>Add Inventory to sell more :) </h1>
            <button onClick={handleAddInventory}>Add Inventory</button>
        </div>
    );
};

export default Inventory;